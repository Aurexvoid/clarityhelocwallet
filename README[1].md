# Clarity Heloc — Digital Wallet

Static site deployed via Cloudflare Pages.

## Deploy

This repo connects to Cloudflare Pages with **no build step**.

- **Build command:** (leave empty)
- **Build output:** `/` (root)
